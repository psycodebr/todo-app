{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Frontend application for to-do list",
  "main": "scripts.js",
  "scripts": {
    "start": "http-server -p 3000 && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "http-server": "^0.12.3"
  },
  "devDependencies": {
    "react-scripts": "^5.0.0"
  }
}